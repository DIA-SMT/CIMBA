# deps
node_modules/
.pnpm-store/

# next
.next/
out/
next-env.d.ts

# builds
dist/
.turbo/

# env — nunca commitear credenciales
.env
.env.local
.env*.local
# Cualquier copia de trabajo del .env. Una vez se coló un .env.bak con la
# cadena de conexión a producción adentro; el patrón de arriba no lo tapaba.
.env.*
# Retazos de trabajo de las ediciones por script (viejo/nuevo).
.tmpfix/
!.env.example

# supabase local
supabase/.branches/
supabase/.temp/

# datos normalizados desde archivos fuente (contienen datos personales)
data/normalizado/

# misc
*.tsbuildinfo
.DS_Store
coverage/
playwright-report/
test-results/

# Scripts de consulta ad-hoc a la base (no son parte del producto)
scripts/_*.mjs
